{
  "name": "webflow-dev-env",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild index.js --bundle --outfile=dist/index.js --watch",
    "build-by-file": "node builder.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.18",
    "serve": "^14.0.2",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "@splidejs/splide": "^4.0.7"
  }
}
